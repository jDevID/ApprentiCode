<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:security="http://www.springframework.org/schema/security"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
        http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security-3.0.xsd">

    <security:http auto-config="true">
        <security:intercept-url pattern="/api/auth/**" access="permitAll" />
        <security:intercept-url pattern="/api/v1/roles/**" access="hasRole('ROLE_ADMIN')" />
        <security:intercept-url pattern="/**" access="isAuthenticated()" />
        <security:custom-filter before="FORM_LOGIN_FILTER" ref="JwtAuthenticationFilter" />
    </security:http>

    <bean id="jwtAuthFilter" class="backend.security.config.jwt.JwtAuthenticationFilter">
        <constructor-arg ref="authenticationManager" />
    </bean>

    <bean id="jwtAuthEntryPoint" class="backend.security.config.jwt.JwtAuthenticationEntryPoint" />

    <bean id="jwtService" class="backend.security.config.jwt.JwtService" />

    <security:authentication-manager alias="authenticationManager">
        <security:authentication-provider ref="authenticationProvider" />
    </security:authentication-manager>

    <bean id="authenticationProvider" class="backend.security.config.jwt.JwtAuthenticationProvider">
        <!-- Configure YourJwtAuthenticationProvider properties -->
    </bean>

</beans>

